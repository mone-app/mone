// split_bill_page.dart

import 'package:flutter/material.dart';

class SplitBillPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Split Bill')),
      body: Center(
        child: Text('Halaman untuk membagi tagihan.'),
      ),
    );
  }
}